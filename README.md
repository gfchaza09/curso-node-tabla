# Notas:

Tabla de multiplicar en Node

```
Opciones:
      --help          Muestra ayuda                                   [booleano]
      --version       Muestra número de versión                       [booleano]
  -b, --base          Base de la tabla de multiplicar       [número] [requerido]
  -l, --list          Muestra la tabla en consola    [booleano] [defecto: false]
      --lim, --limit  Limite para obtener la tabla de multiplicar hasta ese     
                      número                              [número] [defecto: 10]

```